fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
openpyxl==3.1.5
pydantic==2.10.4
# HTTP client for the local Ollama text model (Technique Analysis parsing +
# Head Coach synthesis). The Technique Analysis tab no longer processes video,
# so the old CV stack (opencv, mediapipe, ultralytics/torch, insightface,
# onnxruntime, scikit-image) was removed — analysis text is produced elsewhere
# and pasted in.
httpx==0.28.1
