fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
openpyxl==3.1.5
pydantic==2.10.4
# Video Analysis tab: HTTP client for local Ollama, multipart uploads,
# frame decoding + pose estimation. (Pinned for Python 3.12; mediapipe has no
# 3.13 wheels yet — start.bat builds the venv with py -3.12.)
httpx==0.28.1
python-multipart==0.0.20
opencv-python==4.11.0.86
mediapipe==0.10.21
numpy==1.26.4
# Phase 4 ball/table tracking: trained YOLOv8-seg table-ROI model (reused, the
# user fine-tuned it). Pulls torch (CPU build suffices — table detection runs once
# per clip). Lazy-loaded; the app still boots without it (table detection falls
# back to classical colour segmentation). Weights live at data/models/roi_seg.pt.
ultralytics==8.4.62
